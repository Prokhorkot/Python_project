Решить проблему с кодировками (
    1. При шифровании слетает base64 (при преобразовнии байтов в текст)
    2. Если обернуть шифровку в base64, то тогда слетает utf-8(в bytesToString),
        так как исходные байты не являются текстом
)

------------------------РЕШЕНО----------------------------

Задача на следующий раз:

1. Научиться шифровать симметрично
2. Процесс ->{
    1) В самом начале на стороне клиента генерируется ключ для симметричного шифрования
    2) Вместе с паролем и логином в зашифрованном виде будет передаваться этот ключ
    3) В ответе сервер будет отправлять этот токен, но уже в зашифрованном виде
}
3. Переделать метод POST под новый стандарт
    (Он будет возвращать токен. Так же в зашифрованном виде)

------------------------РЕШЕНО----------------------------

Задача на следующий раз:

1. Решить проблему с тем, что токен слишком большой, чтобы шифровать его симметрично
2. Создать общение второго сервера с каждым клиентом в отдельном потоке